/*
  Author:

  Learning Intention:
  The students will direct-wire a simple DC motor, observe how polarity sets the
  spin direction, and explain why real designs use a motor controller (an H-bridge
  or a continuous servo) instead of direct wiring.

  Success Criteria:
    1.  I can direct-wire a simple DC motor to the battery pack
    2.  I understand that reversed polarity is why the motor spins the other way
    3.  I can explain the limitations of direct wiring: no direction, speed or
        braking control from code
    4.  I can explain how an H-bridge reverses a motor electronically, and how a
        continuous servo packages a motor with its own controller

  Hardware:
    One simple DC motor, battery pack, jumper wires or clips.
    This activity uses REAL wiring (no Grove modules): see Bootcamp-basicMotor.png.
    NOTE: this task needs no code. Use this sketch's comments to record your
    observations, then commit them with a photo of your wiring.

  Safety:
    Only connect the battery pack after your teacher has checked your wiring.
    Never wire the motor directly to an Arduino pin.

  Reference images in this folder:
    Bootcamp-basicMotor.png             the direct-wire experiment
    Bootcamp-continuousServoMotor.png   a continuous servo configuration
    motorShield-reference.png           an H-bridge motor shield configuration

  Documentation:
    https://docs.arduino.cc/tutorials/motor-shield-rev3/msr3-controlling-dc-motor

  My Observations:
    Direction with first wiring:
    Direction after swapping the wires:
    Why real designs need a motor controller:
*/

void setup() {

}

void loop() {

}
